# CODEX_START_PROMPT.md

Read `AGENTS.md` completely, then read `PROJECT.md` completely.

Treat:

- `PROJECT.md` as the authoritative product specification;
- `AGENTS.md` as the authoritative execution, QA and Git-delivery contract.

This Home of Fitness repository is an independent product. Do not reuse layouts, architecture, animation systems, components, page order, spacing patterns or design decisions from any previous website unless `PROJECT.md` explicitly requires them.

Start from the current repository state and autonomously deliver the project from audit through release readiness.

You must:

- inspect the repository before editing;
- inspect and classify every local image;
- create the required project-state, decision, blocker, evidence and QA documents;
- define the first 10 major tasks before implementation;
- build the complete React and TypeScript application;
- use semantic HTML, modern CSS and Framer Motion;
- follow the black-and-white brand direction;
- keep typography compact and professionally art-directed;
- make motion a defining feature without repeating generic fade-up animation;
- create project-specific motion choreography;
- implement all routes, membership, classes, trainers, facilities, shop, product pages, persistent cart, contact, FAQ and legal pages;
- use reference websites only as UX quality benchmarks;
- never copy protected content or ship unlicensed imagery;
- use British English;
- never fabricate prices, testimonials, credentials, ratings, addresses, opening hours, stock or payment success;
- centralise business data and record client decisions;
- implement unit/component and Playwright coverage;
- run formatting, lint, type-check, tests and production build;
- commit and push after every predefined batch of 10 major tasks;
- record real command, commit, push and deployment evidence;
- continue through all implementation and review phases;
- fix all Blocker, Critical and Major findings;
- prepare the project for Vercel;
- complete unaffected work when credentials or client-only facts block one action;
- never force-push `main`;
- never weaken TypeScript, lint rules or tests to manufacture a pass;
- never claim a test, push or deployment succeeded unless it actually ran successfully.

Begin now with the repository audit and the first 10-task batch.

Do not stop after planning, scaffolding, a single page or a single phase. Continue until the Definition of Done in `PROJECT.md` is satisfied or a genuine external blocker remains.
